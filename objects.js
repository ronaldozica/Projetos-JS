
let person = {
    name: `John`,
    age: 25,
    isProgrammer: true
}

console.log(person)

console.log(`--------------------------------`)                 // Partition

console.log(person.name)
console.log(person.age)
console.log(person.isProgrammer)

console.log(`--------------------------------`)                 // Partition

console.log(person.name + ` has ` + person.age + ` years.`)
console.log(`${person.name} has ${person.age} years.`)